***********

run_experiments.py:
  --alpha: alpha
    (default: '1.0')
    (a number)
  --brain_data_dir: Brain Data Dir
    (default: '/Users/samiraabnar/Codes/Data/harrypotter/')
  --context_mode: type of context (sentence, block, none)
    (default: 'none')
  --[no]cross_delay: try different train and test delays
    (default: 'false')
  --delays: delay list
    (default: '-6,-4,-2,0')
    (a comma separated list)
  --embedding_dir: path to the file containing the embeddings
  --embedding_type: ELMO: word_emb, lstm_outputs1, lstm_outputs2
    (default: 'lstm_outputs1')
  --fold_id: fold id
    (default: '0')
    (an integer)
  --future_window: window size to the future
    (default: '0')
    (an integer)
  --[no]load_data: load data flag
    (default: 'true')
  --[no]load_encoded_stimuli: load encoded stimuli
    (default: 'true')
  --[no]only_past: window size to the future
    (default: 'true')
  --param_set: which param set to use
  --past_window: window size to the past
    (default: '3')
    (an integer)
  --[no]save_data: save data flag
    (default: 'true')
  --[no]save_encoded_stimuli: save encoded stimuli
    (default: 'true')
  --[no]save_models: save models flag
    (default: 'true')
  --subject_id: subject id
    (default: '1')
    (an integer)
  --text_encoder: <glove|elmo|tf_token|universal_large|google_lm>: which encoder
    to use
    (default: 'glove')

tensorflow_hub.resolver:
  --tfhub_cache_dir: If set, TF-Hub will download and cache Modules into this
    directory. Otherwise it will attempt to find a network path.

absl.flags:
  --flagfile: Insert flag definitions from the given file into the command line.
    (default: '')
  --undefok: comma-separated list of flag names that it is okay to specify on
    the command line even if the program does not define a flag with that name.
    IMPORTANT: flags in this list that have arguments MUST use the --flag=value
    format.
    (default: '')
***********
brain data dir:  /Users/iSam/Codes/Data/harrypotter/
